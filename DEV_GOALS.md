# Ayabot 开发目标

## 项目: B站弹幕机器人 ayabot
## 仓库: yujianke100/Ayabot

---

## 1. UID 精准配置系统

### 1.1 关键词回复支持 UID 白名单
- [ ] 关键词规则可配置 `allowed_uids: list[int]` 字段
- [ ] 仅白名单内的 UID 能触发该关键词回复
- [ ] 留空/未配置时保持现有行为（所有人可触发）

### 1.2 欢迎模板支持 UID 定制
- [ ] 新增 `welcome_templates_for_uids` 配置项（KV 结构: UID → 模板）
- [ ] 配置了专属模板的 UID 进入时使用其模板
- [ ] 未配置的 UID 使用默认欢迎模板

### 1.3 舰长/提督/总督独立欢迎模板
- [ ] 新增 `guard_welcome_template_captain` / `guard_welcome_template_commander` / `guard_welcome_template_governor` 配置项
- [ ] 大航海用户进入时根据其身份等级匹配对应欢迎模板
- [ ] 未配置时使用默认欢迎模板

### 1.4 UID 配置表格界面
- [ ] 在 Web UI 配置页增加"UID 自定义配置"区域
- [ ] 点击后弹出表格界面，支持增删改每条的：
  - UID
  - UNAME（仅展示）
  - 专属关键词（多值）
  - 专属欢迎模板
- [ ] 保存到 config.yaml

---

## 2. 弹幕日志

- [ ] 在房间配置末尾增加"弹幕日志"区域
- [ ] 字段：时间 | 用户 | UID | 内容
- [ ] 开关：`danmaku_log_enabled`（默认关闭）
- [ ] 保留条数：`danmaku_log_max_entries`（默认 1000）
- [ ] 数据存储：SQLite 新表或内存队列
- [ ] Web UI 查看

---

## 3. 账号系统重构

### 3.1 管理员可管理账号
- [ ] 管理员可在 Web UI 新建/编辑/删除账号
- [ ] 账号类型：管理员 / 一般用户
- [ ] 账号字段：用户名、密码、授权直播间列表
- [ ] 增删改接口: `/api/admin/users` CRUD

### 3.2 所有账号可改密码
- [ ] Web UI 增加修改密码功能
- [ ] 需要旧密码验证

### 3.3 权限检查
- [ ] 账号 "wenwen" 的限制：
  - ✅ 仅可修改自己的密码
  - ✅ 仅可查看被授权直播间的送礼排行 + 精美导出
  - ❌ 不可修改机器人/LLM/其他配置
  - ❌ 不可管理其他账号
- [ ] 通用权限模型：一般用户只能看数据和改自己密码

---

## 4. 直播间转发/点赞感谢

- [ ] 调研 B站 API 是否支持监听：直播间分享、直播间点赞事件
- [ ] 点赞逻辑：每个用户每场直播前 50 次点赞感谢一次，之后不再感谢
- [ ] 支持开关和模板配置
- [ ] 如 API 不支持则记录并在文档中说明

---

## 优先级

1. **P0** — 1.1~1.3 UID 精准配置（核心功能）
2. **P0** — 3.3 权限修复（wenwen 账号安全）
3. **P1** — 2 弹幕日志
4. **P1** — 3.1~3.2 账号系统完善
5. **P2** — 1.4 UID 配置表格 UI
6. **P3** — 4 转发/点赞感谢
